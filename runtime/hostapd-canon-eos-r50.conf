# Pi Camera Control Access Point Configuration
# Optimized for Canon EOS R50 compatibility

# Basic interface configuration
interface=ap0
driver=nl80211

# Network settings optimized for Canon cameras
ssid=PiCameraController002
hw_mode=g
channel=6
ieee80211n=1
ht_capab=[SHORT-GI-20]
wmm_enabled=1
beacon_int=100
dtim_period=2
max_num_sta=5

# Authentication
macaddr_acl=0
auth_algs=1
ignore_broadcast_ssid=0

# Security configuration (WPA2-PSK required for Canon)
wpa=2
wpa_passphrase=welcome-to-markys-network
wpa_key_mgmt=WPA-PSK
wpa_pairwise=TKIP CCMP
rsn_pairwise=CCMP
wpa_group_rekey=86400